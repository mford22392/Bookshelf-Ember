<div class="row">
  <div class="col-xs-3">

    <ul>
      {{#each model as |genre|}}
        <li> {{link-to genre.name "genres.genre" genre}}</li>
      {{/each}}
    </ul>

  </div>

  <div class="col-xs-9">
    {{outlet}}
  </div>

</div>